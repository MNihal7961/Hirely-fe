import React from 'react'

interface InterviewReportProps {
    report:any;
}

const InterviewReport: React.FC<InterviewReportProps> = ({ report }) => {
  return (
    <div>InterviewReport</div>
  )
}

export default InterviewReport